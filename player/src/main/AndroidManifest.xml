<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Cast SDK ищет этот класс сам при первом CastContext.getSharedInstance - см.
             NamiCastOptionsProvider. Ключ обязан быть именно "...OPTIONS_PROVIDER_CLASS_NAME" -
             без суффикса "_NAME" SDK кидает IllegalStateException ("must be provided as a
             metadata... with key ...OPTIONS_PROVIDER_CLASS_NAME") и Трансляция вообще не
             инициализируется, независимо от версии Google Play Services на устройстве. -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="dev.nami.player.NamiCastOptionsProvider" />
        <service
            android:name="dev.nami.player.PlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
    </application>
</manifest>
